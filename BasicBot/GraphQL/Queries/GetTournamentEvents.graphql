query GetTournamentEvents($tournamentSlug: String) {
    tournament(slug: $tournamentSlug) {
        id
        name
        events {
            id
            name
            state
        }
    }
}